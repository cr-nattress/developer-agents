Create a Python module called `sandbox_manager.py` that provides utility functions for creating and cleaning up temporary directories.

Include:
- `create_sandbox() -> Path`
- `cleanup_sandbox(path: Path)`

Use Python’s `tempfile` and `shutil` libraries.
